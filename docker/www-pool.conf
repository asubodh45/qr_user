[www]
; Pass all container environment variables through to PHP processes.
; Without this, PHP-FPM clears the environment and env() calls return null,
; causing Laravel to fall back to config defaults (sqlite instead of pgsql).
clear_env = no
